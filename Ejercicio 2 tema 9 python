numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9]

def miFuncion(x):
    if x % 2 !=0:
        return True

    return False

impares = filter(miFuncion, numeros)


from functools import reduce

def suma (a, b):
    print(f'a={a}, b = {b}')
    return a + b

resultado = reduce(suma, impares)

print(resultado)
